DAVID'S INSURANCE HEADLINE UPDATE

Changes:
Good coverage. Better value.

to:
Same coverage. Better value.

Apply from the davidsinsurance repo root:

git apply DAVIDS_SAME_COVERAGE_HEADLINE.patch
git add app/page.tsx
git commit -m "Use Same coverage Better value headline"
git push
